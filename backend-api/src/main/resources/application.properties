spring.application.name=backend-api

# Conexi�n a la base de datos PostgreSQL
spring.datasource.username=${DATABASE_USERNAME_P}
spring.datasource.password=${DATABASE_PASSWORD_P}
spring.datasource.url=${DATABASE_URL_CHURNINSIGHT}
spring.datasource.driver-class-name=org.postgresql.Driver

# --- CONFIGURACI�N EMAIL (Tarea BE-3) ---
# Usamos variables de entorno para no exponer credenciales en el c�digo
spring.mail.host=${HOST_EMAIL_TEMP}
spring.mail.port=${PORT_EMAIL_TEMP}
spring.mail.username=${USERNAME_EMAIL_TEMP}
spring.mail.password=${PASSWORD_EMAIL_TEMP}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# --- CONFIGURACI�N JPA / HIBERNATE ---
# update: crea las tablas autom�ticamente si no existen
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.format-sql=true
# Hibernate 6 (Spring Boot 3) detecta el dialecto solo, pero puedes dejarlo:
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- INTEGRACI�N CON DATA SCIENCE (Tarea BE-4) ---
# Aqu� configuramos la URL donde correr� el equipo de DS su FastAPI/Flask
# Por defecto ser� localhost:8000 (puerto com�n de Python)
api.ds.url=${DS_SERVICE_URL:http://localhost:8000/predict}

# --- DOCUMENTACI�N SWAGGER (Tarea BE-5) ---
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html


# Puerto del servidor
server.port=8080


